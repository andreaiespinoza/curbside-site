'use client';
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#64b5f6',
          },
        secondary: {
            main: '#ffa726',
          },
    },
  });

export default theme;